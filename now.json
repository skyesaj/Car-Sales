{
  "version": 2,
  "name": "src",
  "builds": [
    {
      "src": "src/package.json",
      "use": "@now/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "src/$1" }]
}
